import 'dart:ui';

class AppConfig {
  static const String localePath = 'assets/lang';
  static const List<Locale> supportedLocales = <Locale>[
    Locale('fa', 'IR'),
  ];

  static const Locale persianLocale = Locale('fa', 'IR');

}
